#!/usr/bin/env mongo
// Insert a document into the "school" collection in the "my_db" database
db = db.getSiblingDB('my_db'); // Switch to the my_db database
db.school.insertOne({ name: "Holberton school" }); // Insert the document
